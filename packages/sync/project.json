{
  "name": "sync",
  "root": "packages/sync",
  "sourceRoot": "packages/sync/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run build",
        "cwd": "packages/sync"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm test",
        "cwd": "packages/sync"
      }
    }
  }
}
