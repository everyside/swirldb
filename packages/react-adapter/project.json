{
  "name": "react-adapter",
  "root": "packages/react-adapter",
  "sourceRoot": "packages/react-adapter/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run build",
        "cwd": "packages/react-adapter"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run",
        "cwd": "packages/react-adapter"
      }
    }
  }
}
